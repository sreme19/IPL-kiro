# SAM excludes (relative to CodeUri = lambda_package/).
# data/ is intentionally NOT listed — api/routers/matches.py loads data/ipl.duckdb
# at runtime and it must ship inside the Lambda zip.
.pytest_cache/
*.pyc
__pycache__/
.git/
.gitignore
.env
.DS_Store
node_modules/
.aws-sam/
dist/
