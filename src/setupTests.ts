/**
 * src/setupTests.ts
 * ------------------
 * Loaded automatically before every Vitest run (configured in vite.config.ts).
 * Extends expect() with jest-dom matchers like toBeInTheDocument().
 */
import '@testing-library/jest-dom'
